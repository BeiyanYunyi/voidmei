# 单窗口、多区域 HUD 可行性调研

目标：所有运行中的 HUD 元素位于同一个鼠标穿透的顶层窗口内，在不同屏幕区域排列，各区域可独立设置透明度。设置窗口可以独立存在。此文保留调研依据与整体方案；初步实现进度见文末。

## 当前状态速览

当前源码已实现单窗口分区模式，入口为 HUD 设置中的“使用单窗口分区布局（试验性）”。可选择显示器覆盖范围，并在预览中排列、缩放区域；飞行、发动机、姿态、机械化、告警、游戏消息、地图对象、准星、罗盘和操纵面都在同一个 HUD 顶层窗口中绘制。设置和编辑预览本身仍是独立窗口，不属于运行中的 HUD 分区。

每区的背景不透明度与内容不透明度分别配置。例如，速度区域可设背景 0、内容 1，发动机区域设背景 0.5、内容 1；两者之间的空白完全透明，整个 HUD 统一鼠标穿透。这里的透明度属于区域绘制的逐像素 alpha，不应通过原生窗口的整体 opacity 实现，否则文字和所有区域都会一起变淡。

已记录的隔离测试覆盖区域 alpha、单一原生 HUD 窗口身份、兼容路径穿透，以及 1080p／4K 软件路径测量。真实游戏中的新分区布局、物理多屏混合 DPI 和原生 Wayland 穿透尚未完成验收。以下早期阶段记录中的“尚未实现”需结合后续章节阅读。

## 结论与依据

可行。需要的是透明顶层窗口中的逐像素 alpha 与组件布局，而不是给每个 HUD 建立一个原生窗口。Compose 的 Box、Column、Canvas 和文字组件可以在同一内容树内组合；组件分组本身不创建顶层窗口。

- [Kotlin 官方透明窗口说明](https://kotlinlang.org/docs/multiplatform/compose-desktop-top-level-windows-management.html#create-transparent-windows)：桌面 Window 使用 transparent=true、undecorated=true。
- [Compose 官方图形说明](https://developer.android.com/develop/ui/compose/graphics/draw/modifiers)：颜色 alpha 可以控制局部绘制；graphicsLayer 的 alpha 可以整体合成一个组件组，默认策略在需要时使用离屏层。不能把每次绘制分别调低 alpha 与整个组统一淡化混为一谈，重叠部分的合成结果不同。
- 当前 `HudWindow.kt` 已在原生和兼容两条路径间选择一个 HUD 顶层窗口。
- 当前 `SwingGraphicsHudWindow.kt` 的 JFrame 背景透明；`BufferedHudComposePanel.kt` 使用 ARGB_PRE 缓冲、逐帧清空及 Src 整帧替换，保留每个像素的 alpha，避免旧像素残留。这与分散组件兼容。
- `HudPanel.kt` 的纵向模式给整个容器绘制单一 hudOpacity 背景并纵向滚动；分区模式则交给 `HudScene.kt`，各区域分别绘制背景和内容 alpha。这是布局选择，不是透明窗口能力限制。

## 建议的运行结构

一个无边框、透明、置顶、不获取键盘焦点的 HUD 窗口，覆盖用户选定显示器的目标区域；窗口根容器不填充背景。飞行读数、发动机读数、姿态、机械化、消息、告警和准星成为容器内的组件组，各自拥有位置、尺寸和样式。

第一阶段以单显示器为目标，使用稳定的屏幕覆盖边界，不随每次读数或告警内容改变原生窗口尺寸。当前 `updateHudWindowSize` 的内容驱动尺寸、1000 dp 宽度及 900 dp 高度上限不能直接用于这种模式。使用普通无边框覆盖窗口定位尺寸，不需要进入操作系统独占全屏。

每组建议保存：稳定实例 ID、组件类型、锚点、dp 偏移、尺寸、层叠顺序、背景 alpha、内容 alpha，以及字段／发动机等内容选项。同种组件可有多个实例，例如分别显示发动机 1 和 2；遥测、模型及告警仍共享同一应用会话，组件实例不各自启动轮询。

背景与内容透明度应分开：可以背景完全透明而文字保持清晰，也可以将整组内容一起淡化。父容器不施加整体 alpha，避免无意中连乘所有子组件的透明度。空白区域 alpha 为零。重叠组按明确层叠顺序正常合成。

## 鼠标穿透与编辑

视觉透明与输入穿透相互独立。即使某区域绘制不透明，整个 HUD 仍可穿透；透明像素也不能单凭颜色保证穿透。

当前 `HudPointerInput.kt` 已统一控制整窗输入：Linux 路径用 X11 SHAPE 空输入区域，Windows 使用现有平台实现；macOS 目前明确不支持该项，原生 Wayland 路径也尚未实现。不能把当前 X11 验证直接推广到所有 NixOS 桌面会话。

运行时整窗穿透；布局编辑优先在设置窗口的缩放预览内完成，拖动修改组件坐标，真实 HUD 同步显示。这样不依赖热键，也无需让覆盖整屏的 HUD 临时拦截鼠标。将来如果增加直接编辑模式，必须有设置／托盘上的可靠退出入口。

## 成本与待验证项

窗口数量减少不保证渲染成本下降。兼容路径当前每帧清空并复制整个缓冲：3840×2160 的单个四字节像素缓冲约 31.6 MiB，80 ms 更新间隔下，仅一次整帧写入的量级就约 396 MiB/s，尚未计清空、其他绘制／复制与合成。应先测量全屏尺寸的 CPU、帧耗时、内存和更新抖动，再决定是否优化绘制范围；保留已修复闪烁的整帧呈现语义。

跨显示器的单一矩形窗口理论上可覆盖多个屏幕的包围矩形，但混合 DPI、负坐标、屏幕间空洞和热插拔都需专门处理，不能简单累加当前窗口宽度。透明置顶能否覆盖游戏还受窗口系统及游戏显示模式影响；当前用户验证的兼容 HUD 可作为基础，但不能替代新全屏布局实测。

## 实施与验收顺序

本轮复跑两个既有专用原生测试，通过（`/tmp/voidmei-single-window-research-after.log`，5 秒）：`NativeHudTransparencyTest.transparentAndTranslucentPixelsRevealChangingBackground` 在同一兼容窗口绘制 alpha 0、0.5、1 三个区域，Robot 像素采样验证底层从红色变成蓝色时的合成结果及内容隐藏／恢复；`NativeHudPointerTest.swingGraphicsHudPassesClicksThroughAndRestoresInput` 验证兼容窗口穿透与恢复输入。环境为隔离 Xvfb/xcompmgr，不是用户物理游戏桌面。这两项分别验证基础能力，尚未验证全屏多组件布局。首次误用只包含 GuiTest 的任务，未找到测试；改用仓库专用 nativeHudTransparencyTest／nativeHudPointerTest 后执行成功。

1. 建立共享的组件布局数据与坐标计算；将已有布局迁移成等价的初始组件组，保留字段和样式。
2. 在一个兼容 HUD 窗口中放置分散的实际组件，验证不同背景／内容 alpha、透明间隙、动态刷新、隐藏后无残影，以及显示 HUD 的顶层窗口数为一。
3. 验证空白区和有内容区都将鼠标点击传给下层窗口；保持不抢焦点。
4. 添加设置中的布局编辑预览与持久化，再补显示器选择、分辨率变化和 DPI 恢复策略。
5. 用用户的 NixOS 兼容路径测量全屏更新成本，并验证游戏中的位置、透明和穿透；其他平台分别验收。

热键继续后置。此方案不要求为每个 HUD 组件创建 Window、JFrame 或独立的渲染进程。

## 第一阶段实现

HUD 设置新增“使用单窗口分区布局（试验性）”。共享 HudSceneLayout 保存画布尺寸、启用状态及按层叠顺序排列的区域；每区保存 ID、内容类型、坐标、尺寸、两种 alpha 和发动机编号。配置拒绝越界、重复 ID、非法透明度及未知内容类型。切回纵向模式仅禁用分区，保留其配置，重新开启或重启后仍可恢复；恢复默认布局及撤销重置包含分区配置。

HudScene 在现有 HudWindow 内布局实际飞行、发动机、姿态、机械化和告警组件，底层透明，各区分别绘制背景和内容 alpha。准星仍在整窗层呈现。分区模式强制使用现有整窗鼠标穿透，不修改用户原先的纵向模式穿透偏好。窗口尺寸按画布和当前可用空间确定，不随每帧读数变化；空间不足时等比缩小组件坐标及显示尺寸。

当前初始画布为 1280×720 dp，以现有 HUD 开关和发动机选择建立初始区域。设置中可用滑块调整位置、尺寸、背景／内容不透明度，独立预览沿用相同渲染。第一阶段尚未实现区域增删与画布尺寸界面，后续进度（包括预览编辑和显示器覆盖）见下文。锚点和跨多个显示器的统一布局仍待实现；全屏性能尚未验收。

共享 JVM／JS、桌面单元及完整 GUI 回归通过（`/tmp/voidmei-hud-scene-complete.log`，51 秒）；GUI 共 74 个类、199 项，零失败／错误／跳过。覆盖配置往返与边界、模式切换保留、真实区域位置与 alpha、延迟清除旧值；原生兼容窗口测试验证区域更新前后仅有一个同一身份的 HUD Frame。原生截图 `desktop/build/hud-preview/single-window-regions.png` 已检查：分散区域、不同背景透明度及实际读数可见。最初原生测试错误地查询兼容 ComposePanel 的全局测试语义，后改为在正确组合范围观察更新、检查 Frame 与原生截图；读数与 alpha 断言由直接组合测试覆盖。

最终默认 Kotlin 离线包构建通过（`/tmp/voidmei-hud-scene-nix-final.log`）。以上是隔离 Xvfb/xcompmgr 验证。用户此前的兼容 HUD 真机验收不自动覆盖此新布局；物理游戏、全屏更新成本和跨平台行为仍待验证。

## 区域增删与发动机选择

分区设置现可添加十种区域（含后续加入的游戏消息、地图对象、准星、罗盘和操纵面），可重复添加同类内容。新区域生成未占用的稳定 ID，在画布允许范围内错开初始位置；画布很小时收缩初始尺寸以保证不越界。最多 32 个区域，至少保留一个；移除仅影响指定区域。发动机区域默认选择尚未使用的正整数编号，并提供独立编号输入；非法输入不覆盖已保存值。字段清单默认共享现有飞行／发动机字段设置，不自动分配到其他发动机；独立字段选择见下文。

共享 JVM／JS、桌面单元与区域 GUI 回归通过（`/tmp/voidmei-hud-scene-regions.log`，10 秒）；补充双发动机显示后五项区域 GUI 测试通过（`/tmp/voidmei-hud-scene-regions-final.log`，2 秒）。验证唯一 ID、小画布边界、32 个上限、最后一个区域不可移除、配置往返、无效编号、指定区域移除，以及两台发动机各自读数和单侧数据缺失时的隔离。沿用并通过单原生窗口数量及 alpha 测试。

默认离线 Nix 构建通过（`/tmp/voidmei-hud-region-editor-nix.log`）。本轮未扩大真实游戏或其他平台验收结论。

## 画布尺寸调整

分区设置新增画布宽度／高度输入与“应用画布尺寸”。采用 dp，分别允许 240–8192 与 120–8192；输入过程中不改变实际窗口或区域，两项有效后一次应用。共享 resizeCanvas 在放大时保留几何信息，在缩小时仅缩小放不下的区域并将越界位置移回边界；保留 ID、类型、层叠顺序、发动机编号、透明度及启用状态。输入期间其他区域编辑仍保留，不用旧快照覆盖。

共享 JVM／JS、桌面单元及六项相关 GUI 回归通过（`/tmp/voidmei-hud-canvas-size.log`，10 秒），覆盖非法尺寸、角落区域移动、区域大于画布、放大不变形、原子应用、编辑期间透明度变化和配置往返，以及既有区域管理与显示。本轮未重建 Nix 包，自动覆盖显示器和跨屏策略仍待完成。

## 每区独立读数字段

飞行和发动机区域增加可选 fields 清单。null／旧配置缺失此键时继承全局 HUD 字段，空清单表示明确不显示读数；未知 ID、顺序和配置往返保持。渲染使用区域清单，包括航向字段所附带的地图格网；各区域复用同一遥测与模型会话。

设置中开启“此区域独立选择字段”时复制当前全局选择，此后独立搜索、添加、移除、排序；关闭时恢复全局继承。未知字段标出其 ID，编辑其他字段时不移除。独立清单不修改全局字段，区域位置和尺寸变化不改变清单。

共享 JVM／JS、桌面单元及七项相关 GUI 回归通过（`/tmp/voidmei-hud-region-fields.log`，11 秒），验证两个飞行区域分别显示 IAS／高度、全局改为 TAS 时独立区域保持 IAS、发动机区域独立显示温度、空清单不继承，以及未知字段保留、搜索、顺序编辑和恢复继承。原有区域管理、双发动机、单窗口数量与 alpha 测试继续通过。

默认离线 Nix 包构建通过（`/tmp/voidmei-hud-region-fields-nix.log`），包含此前画布尺寸设置。本轮未新增真实游戏验收。

## 重叠区域层叠顺序

分区设置明确列表从底层到顶层排列，新增“上移一层／下移一层”，边界处禁用对应按钮。共享模型只改变区域列表顺序，保留区域 ID、字段、透明度、位置、尺寸和发动机编号，保存后沿用该绘制顺序。

共享 JVM／JS、桌面单元及六项相关 GUI 回归通过（`/tmp/voidmei-hud-region-layers.log`，11 秒）。GUI 从实际设置按钮调整两个重叠区域，截取速度读数位置的像素，验证不透明背景遮挡、上移后白色读数出现、下移后再次遮挡，并验证配置往返及完整区域属性保持。原有增删、双发动机、独立 alpha 和单原生窗口测试继续通过。本轮未重建 Nix 包，未新增真实游戏验收。

## 预览中拖动区域

设置打开的预览窗口新增“拖动区域”。开启后用边框显示所有分区，包括完全透明、空字段及当前隐藏的告警区域；按下时按实际层叠顺序选取顶层区域，拖动经预览缩放及显示密度换算成画布坐标，并限制在画布内。编辑层只存在于预览窗口，更新通过原有设置保存流程传回真实 HUD；关闭编辑后移除输入层。真实 HUD 继续穿透，区域类型、大小、样式及字段保持。

初始采用通用拖动检测器时测试发现起拖阈值损失位移，已改为从按下位置连续跟踪。共享 JVM／JS、桌面单元通过；定向 GUI 验证半比例预览的精确位移、透明顶层选取、画布边界、空白处拖动无效果、配置往返及关闭编辑。真实独立预览测试从设置入口开启编辑并注入拖动，验证新位置保存及 HUD 开关／穿透偏好不变（`/tmp/voidmei-hud-region-drag-window.log`）。

最终完整桌面回归及离线 Nix 构建分别见 `/tmp/voidmei-hud-drag-full-gui.log` 和 `/tmp/voidmei-hud-region-drag-nix.log`，均通过；GUI 共 78 个类、207 项，零失败／错误／跳过，49 秒。隔离 Xvfb/xcompmgr 验证，不扩大真实游戏或跨平台验收结论。该阶段仅支持拖动位置，缩放见后续记录。

## 预览中拖动缩放

编辑模式在区域右下角显示 12 dp 方块（小区域按显示尺寸收缩）。按下时先选顶层区域，再判断是移动还是缩放，整个手势保持该操作。右下角拖动保持左上角坐标，只改变宽高，沿用预览缩放／显示密度换算；最小为 80×40 dp，最大为从当前原点到画布边界的剩余空间。设置保存、真实 HUD 同步及原有滑块继续共用区域配置。

共享 JVM／JS、桌面单元及四项相关 GUI 回归通过（`/tmp/voidmei-hud-region-resize.log`，11 秒）。覆盖半比例预览精确缩放、两端尺寸限制、原点／字段／样式保留与配置往返；真实预览从设置入口拖动右下角后保存 340×220 dp，位置保持，原有移动及预览模式回归通过。本轮未重建 Nix 包，未增加真实游戏验收。

## 选择显示器并自动覆盖

分区设置新增“按画布尺寸”及当前显示器选择。选定显示器后，现有 HUD 窗口覆盖其完整 AWT 边界（含任务栏区域），采用普通透明置顶窗口，不进入独占全屏，也不创建额外 HUD 窗口。画布等比放大／缩小适配，宽高比不同时右侧或底部保留透明空白，预览编辑使用相同缩放规则。切回画布模式后恢复配置尺寸，当前位置沿用覆盖后的窗口原点。

显示器 ID 随布局保存。运行时每秒独立检查显示器列表；目标不存在则临时回退主屏（或列表首屏），保留原 ID 以便重新可用时恢复。设置中的显示器列表提供手动刷新。覆盖模式移除原有工作区限高及位置修正监听，避免完整屏幕边界与任务栏避让互相争夺位置；退出时停止覆盖定时器。坐标换算沿用当前 HUD 桥接的 AWT 用户坐标→设备像素→Compose dp 规则。

共享 JVM／JS、桌面单元和定向 GUI 通过（`/tmp/voidmei-hud-display-placement-after.log`，11 秒）。合成显示器测试覆盖负坐标、非均匀缩放、显式目标与主屏回退；隔离 X11 原生窗口测试通过真实设置按钮选择显示器、检查完整边界、模拟不可用 ID 回退、切回 400×250 画布并跨定时周期保持尺寸，始终为同一 HUD Frame。

最终完整 GUI 通过：79 类、209 项，零失败／错误／跳过，43 秒（`/tmp/voidmei-hud-display-full-gui-after.log`）。首轮完整回归中，已有预览测试在关闭编辑时发生注入超时，改用该按钮的语义动作验证模式关闭，实际拖动／缩放输入测试保留。默认离线 Nix 构建通过（`/tmp/voidmei-hud-display-nix.log`），包含此前拖拽缩放功能。

这些结果不证明物理热插拔、混合 DPI 多屏或真实游戏的全屏叠加／刷新性能；显示器 ID 的跨系统重启稳定性取决于平台。当前模式覆盖一个选定显示器，跨多屏单窗口布局仍待实现。

## 全显示器兼容路径测量

新增显式运行的 `:desktop:nativeHudScenePerformanceTest`，默认桌面单元测试不执行。隔离脚本支持 `VOIDMEI_TEST_SCREEN_SIZE=1920x1080` 或 `3840x2160`；默认仍为 1280×900。测试使用当前生产 HudWindow、显示器定位、HudPanel 与分区布局，显示飞行、发动机、姿态和机械化内容，每 80 ms 更新 IAS 和一个用于检测呈现变化的不透明标记。测量窗口约 5 秒，逐次检查原生窗口身份、边界和标记像素；Robot 采样间隔至少 10 ms。

本机 Xvfb/xcompmgr、SOFTWARE_FAST 两次顺序运行结果如下。对应生产实现为 `5b200d2`，测试通过源码测试运行时执行，并非打包启动或物理 GPU 测试。

| 观测项 | 1920×1080 | 3840×2160 |
| --- | ---: | ---: |
| 测量时长 | 5011 ms | 5104 ms |
| 80 ms 定时回调次数 | 62 | 62 |
| 回调间隔 P50 / P95 | 80.08 / 80.14 ms | 81.12 / 83.74 ms |
| 屏幕采样次数 / 观测颜色变化次数 | 373 / 62 | 62 / 56 |
| 测试进程 CPU（一个逻辑核心=100%） | 62.3% | 101.7% |
| 采样峰值已用 JVM 堆 | 47.4 MiB | 97.3 MiB |
| 单个四字节像素整窗缓冲的计算大小 | 7.91 MiB | 31.64 MiB |

两次测量均未在采样点发现不透明标记消失，窗口边界和身份保持。4K 路径的软件绘制／呈现成本更高，需进一步分离分析渲染、缓冲复制和屏幕采样成本。回调间隔不是呈现帧率；4K 下 Robot 读屏明显变慢，56 次颜色变化不能解释为丢失 6 帧。CPU 包含测试进程内的界面、采样、JUnit 等开销，不包括外部 Xvfb／xcompmgr；JVM 堆不包含全部原生／GPU 内存。短时单次观测不能证明长期稳定性或精确性能差异。

初次 1080p 测量没有采样限频，5 秒内读屏超过 13000 次；该 CPU 数据已排除，改用限频后两组数据。最终日志为 `/tmp/voidmei-hud-perf-1080p-sampled.log`、`/tmp/voidmei-hud-perf-4k.log`，含测量值的报告副本为 `/tmp/voidmei-hud-perf-1080p.xml`、`/tmp/voidmei-hud-perf-4k.xml`。Python 脚本语法检查通过。本轮仅增加测试工具与测量记录，未改运行绘制实现或重建 Nix 包，不扩大用户 NVIDIA／OPENGL 和真实游戏验收结论。

## 游戏消息分区与共享读取

新增 MESSAGES 区域，在同一 HUD 窗口内显示最近接收的五条事件／损伤消息，保留类别和原始编号，不将跨类别编号解释为时间顺序。请求失败时提示并保留已有记录；遥测延迟时区域显示连接状态，不展示旧消息，但应用层会话及游标跨短暂延迟保留。

Main 使用应用层 HudMessageSession，让设置页与所有消息区域共用一个读取会话。仅在飞行会话有效且 HUD 需要消息或设置页展开时读取；真正断线、换机、换连接／重连代次或手动重读时清空／重建。设置页“重新读取”明确作用于共享历史。预览用标记为示例的本地事件／损伤消息，缺失模式为空，不启动网络请求。

共享 JVM／JS、桌面单元及相关 GUI 回归通过（`/tmp/voidmei-hud-message-regions.log`，13 秒）。共享会话补充回归通过（`/tmp/voidmei-hud-shared-messages-final.log`，1 秒）：注入计数消息源，两个 HUD 消费者与实际设置页面板同时显示时启动次数／活动会话均为一；验证短暂延迟不重启、手动重读替换源、断线取消并清空，以及最近五条和错误保留显示。原有 HTTP 消息面板、区域管理与预览回归继续通过。

默认离线 Nix 包构建通过（`/tmp/voidmei-hud-message-regions-nix.log`）。本轮未新增真实游戏消息验收。

## 分区溢出提示与滚动状态

分区内容复用 HUD 的可视滚动位置提示，放在区域内的右侧留白中，与内容使用相同 alpha；内容能完整显示时移除提示。普通遥测变化保留滚动位置，飞行会话、机型、区域类型、发动机编号或有效字段清单变化时回到顶部，避免切换内容后仍停留在旧列表中段。告警保留其现有内部滚动提示。真实 HUD 继续穿透；可在预览退出拖动模式后滚动检查，或放大区域／减少字段使其完整显示。

桌面单元与相关分区 GUI 回归通过（`/tmp/voidmei-hud-region-scroll.log`，8 秒），验证底部读数可滚动到达、普通 RPM 更新保留位置、换发动机和反转字段顺序回到顶部、增大区域后提示消失。独立字段、层叠遮挡、拖动缩放、消息共享与原生单窗口回归继续通过。本轮未重建 Nix 包，未新增真实游戏验收。

## 独立告警区域使用配置高度

修正 FlightAlertPanel 内部再次截断最大高度为 112 dp 的问题。高度预算现在由容器决定：纵向 HUD 保持原有自适应上限，独立区域则使用自身高度扣除内边距后的空间。区域较小时仍滚动，告警消失后仍隐藏区域。

桌面单元及八项相关 GUI 回归通过（`/tmp/voidmei-hud-alert-region-height-after.log`，7 秒）。验证放大区域后全部十条告警可见且实际列表高度超过 112 dp、缩小后末条可滚动到达，以及纵向矮窗口的读数空间保护、预览和分区布局。首次测试使用了当前 Dp 边界类型不存在的 height 属性，已按项目现有 bottom−top 方式修正。本轮未重建 Nix 包，未新增真实游戏验收。

## 分区显示开关

各区域新增“显示此区域”。关闭后移除该区背景和内容绘制，保留位置、尺寸、透明度、字段、发动机编号及层叠顺序；重新显示或重启后沿用保存配置。旧配置缺少 visible 时默认显示。预览编辑模式仍显示隐藏区域的边框，并标注“已隐藏”，以便继续调整；普通预览与实际 HUD 都隐藏其内容。

消息读取需求仅计算启用 HUD 中可见的消息区域。隐藏最后一个可见消息区域会取消读取，设置页展开消息时仍保持共享会话；存在其他可见消息区域时不重启读取。隐藏不会改变真实 HUD 的整窗穿透行为。

共享 JVM／JS、桌面单元和定向 GUI 测试通过（`/tmp/voidmei-hud-region-visibility.log`，12 秒）。新增回归覆盖旧配置默认值与非法类型、隐藏配置往返、实际设置开关隐藏／恢复发动机读数、编辑标注，以及多个消息区域与设置页面板之间的读取启停。

完整 GUI 回归通过：83 类、215 项，零失败／错误／跳过，45 秒（`/tmp/voidmei-hud-visibility-full-gui.log`）。默认 Kotlin 离线 Nix 包构建通过（`/tmp/voidmei-hud-visibility-nix.log`），包含此前分区滚动提示和告警区域高度修正。本轮没有新增真实游戏验收。

## 隐藏后继续读取消息

修正消息需求恢复时重新从零游标读取的问题。共享会话将当前历史与事件／损伤游标传给新读取器；恢复时先保留原历史，再请求游标之后的消息，不再先发出空列表。两类游标独立于最多 200 条的显示历史保存，即使某类消息已被挤出历史，其游标仍不回退。请求失败保留历史和游标；真正断线、换机、连接代次变化及手动重新读取仍使用空状态。

共享 JVM／JS、桌面单元及相关 GUI 回归通过（`/tmp/voidmei-hud-message-resume.log`，12 秒）。共享测试验证历史截断后暂停恢复、失败重试的实际请求游标与去重；GUI 验证暂停时取消读取、恢复传递原状态、手动重读及断线后重新飞行传递空状态。既有共享消费者、消息显示和区域显隐测试继续通过。本轮未重建 Nix 包，未新增真实游戏验收。

## 地图对象分区

新增“地图对象”区域，默认高度 500 dp，沿用区域增删、显隐、透明度和布局配置。在同一 HUD 内展示玩家格号、归一化对象位置、方向、网格和距离标尺，复用原地图绘图及宽高比例／边界裁剪逻辑。当前为明确标注的不含底图的示意图；HUD 关闭点击选择，不提示点击详情。设置页的原有底图与点击选择功能保持。

所有地图区域使用 Main 已有的共享地图流，与设置页及航向格号共用读取；区域隐藏或遥测非飞行时取消对应订阅。地图失败、等待和连接中显示状态，不保留旧对象；遥测延迟时显示遥测状态。图面尺寸随区域可用宽度和高度预算调整，其他说明超出区域时沿用滚动提示。

预览提供本地玩家、飞机与机场线段示例，不连接游戏；缺失模式显示等待地图。共享 JVM／JS、桌面单元与相关 GUI 回归通过（`/tmp/voidmei-hud-map-regions-after.log`，13 秒），覆盖区域重复添加与配置往返、两区共用地图更新、只读点击、错误／延迟清除、隐藏取消订阅及预览切换，并回归原设置页地图与线段绘制。首次编译的布局作用域访问错误已修正。

默认 Kotlin 离线 Nix 包构建通过（`/tmp/voidmei-hud-map-regions-nix.log`），同时包含此前消息暂停恢复修正。定向 GUI 共 10 项通过；本轮未新增真实游戏地图验收。

## 窄区域中的紧凑地图展示

地图 HUD 使用紧凑标题，保留对象数与“无底图”标记，将玩家归一化位置移到图面前，与格号一起优先展示；详细坐标／裁剪说明仍在设置页地图中提供。图面高度预算随 HUD 字体缩放预留更多文字空间，距离标尺继续按实际图面尺寸绘制。该调整不改变地图对象或比例计算，也不改变设置页的交互布局。

新增 240×400 dp 区域的 1×／1.5×／2× 字体回归，检查玩家位置、图面与标尺完整位于区域边界内。修正前测试因玩家位置不可见失败（`/tmp/voidmei-hud-map-compact-before.log`）；修正后桌面单元与地图 GUI 回归通过（`/tmp/voidmei-hud-map-compact.log`，8 秒）。更小区域或更多换行仍可能需要放大区域，保留原有溢出提示。本轮未重建 Nix 包，未新增真机验收。

## 兼容窗口地图像素回归

新增 HudMapNativeGuiTest，在隔离 Xvfb/xcompmgr 中创建固定颜色的下层窗口与一个穿透的兼容 HUD，绘制两个地图分区。Robot 读取实际屏幕像素，验证两个玩家标记随共享数据向左移动、原位置不残留标记；隐藏左区后逐像素检查整块区域恢复下层背景色，右区仍显示，随后恢复左区。整个过程保持同一个 HUD Window，存活 HUD Frame 数为一。

截图检查发现本地示例的网格原点设在下边界，使格号未知；已按现有行号向南递增的模型改为上边界，示例中央位置显示 F6，并增加 UI 断言。游戏数据解析和格号算法不变。最终桌面单元与四项地图 GUI 测试通过（`/tmp/voidmei-hud-map-native-grid.log`，5 秒），截图 `desktop/build/hud-preview/map-regions-native.png` 已检查。此前初始及加强版原生像素测试也通过；本轮未重建 Nix 包，这些隔离验证不替代真实游戏验收。

## 告警区域的连接异常提示

修正只使用告警区域时无法看见连接异常的问题。非飞行状态下，可见的告警区域与其他区域一样优先显示连接状态，即使告警列表为空也不自动消失；若旧告警列表尚未清空，也不继续绘制它。恢复飞行后显示当前告警，无告警时恢复自动隐藏；用户显式关闭区域时仍保持隐藏。

桌面单元及相关分区 GUI 回归通过（`/tmp/voidmei-hud-alert-connection.log`，7 秒）。新增测试覆盖连接中、遥测延迟、断线、空列表／旧告警列表、恢复飞行及显式隐藏；既有区域高度、布局与原生地图像素测试继续通过。

完整 GUI 回归通过：85 类、221 项，零失败／错误／跳过，44 秒（`/tmp/voidmei-hud-alert-connection-full-gui.log`）。默认 Kotlin 离线 Nix 构建通过（`/tmp/voidmei-hud-alert-connection-nix.log`），包含近期紧凑地图和预览格号修正。本轮未新增真实游戏验收。

## 恢复上次移除的区域

分区设置提供“恢复上次移除的区域”，保存当前设置页内最近移除的完整区域与原层号。收起分区设置仍可恢复；记录不写入配置文件，再次移除会替换记录。恢复只插入该区域，保留之后对其他区域、画布和非布局设置的修改；当前画布变小时按边界调整位置和尺寸，原 ID 已占用时生成未使用的区域 ID，原层号超出列表时放到末尾。32 个区域上限时禁用恢复并提示，不自动移除其他区域。

共享 JVM／JS、桌面单元及相关 GUI 回归通过（`/tmp/voidmei-hud-region-restore.log`，11 秒）。验证完整属性与原层号恢复、编号复用、缩小画布、数量限制、配置往返，以及实际移除／新增／折叠／恢复操作中保留后来修改的背景透明度和其他区域。本轮未重建 Nix 包，未新增真实游戏验收。

## 独立准星区域

新增“准星”区域，初始尺寸 128×128 dp，背景默认全透明。区域在现有窗口内居中绘制线框准星或全局选择的自定义图片，按区域尺寸缩放，使用区域内容透明度；位置、层叠顺序、显隐与预览拖动缩放沿用分区编辑。图片的保持比例／拉伸选项继续有效。

布局中存在准星区域时，取代原先整窗层准星，由各区域显示开关控制；即使该区隐藏也不恢复整窗准星，防止重影。移除全部准星区域后恢复原有整窗设置。原有准星大小／靠右选项仅控制整窗准星，不改变区域中心与尺寸；旧配置没有准星区域，行为保持。遥测非飞行时区域显示连接状态，不绘制准星。

共享 JVM／JS、桌面单元及定向 GUI 回归通过（`/tmp/voidmei-hud-crosshair-region.log`，11 秒），验证透明默认值、配置往返、实际准星位置、像素绘制宽度随缩放增加、替换整窗准星、显式隐藏、移除后恢复及延迟清除。

图片准星和预览拖动缩放回归通过（`/tmp/voidmei-hud-crosshair-region-compat.log`，2 秒）；默认 Kotlin 离线 Nix 包构建通过（`/tmp/voidmei-hud-crosshair-region-nix.log`），包含上一阶段区域恢复功能。本轮未新增真实游戏准星验收。

## 准星图片共享与完整替换

分区场景将同一路径的准星图片状态提升到场景内共享，所有可见准星区域共用一次文件轮询与解码；没有可见准星需求或遥测非飞行时取消读取。原纵向准星继续可独立使用同一加载组件。

同一路径更新时保留当前完整图片，直到替代图片解码成功后整体发布，不再先清空图片制造短暂空白。解码失败仍清除旧图并提示；更换路径通过原有组合身份重置，不继承旧路径图片。文件属性轮询与每秒检查规则保持。

桌面单元、图片准星与分区 GUI 回归通过（`/tmp/voidmei-crosshair-shared-image.log`，7 秒）。新增受控解码测试让两个消费者共享加载状态，验证首次加载一次、暂停第二次解码时两处旧图保持、完成后两处更新且总共只加载两次；既有文件删除／恢复、坏图片替换、图片拉伸及准星分区回归继续通过。本轮未重建 Nix 包，未新增真实游戏验收。

## 离线包的单窗口分区验收

`script/smoke_kmp_recording.py` 新增 `--hud-scene`，要求开启 HUD、正常退出测试及 1× 显示缩放。临时配置放置飞行、发动机、消息、地图和准星五区，模拟 HTTP 提供相应数据；至少读取三次地图对象和消息后才能结束。测试代理在 HUD 达到配置的 900×600 尺寸后，每 250 ms 检查同一原生 Frame、单窗口数量、可见性和固定边界。退出前截图并检查可见黄色标记，再沿原有路径关闭主窗口、保存位置和停止记录。

最新离线包构建通过（`/tmp/voidmei-scene-package-build.log`）。实际包在隔离 Xvfb/xcompmgr、SOFTWARE_FAST 下以兼容模式和 80 ms 间隔启动，加入第 20 次请求延迟 1.5 秒的场景，最终验收通过（`/tmp/voidmei-scene-package-smoke-final.log`）：87 组配对 CSV 样本、同一文件对、9 次地图对象读取、9 次消息读取（首次零游标，此后事件游标为 1）；窗口稳定检查 34 次，AWT 心跳延迟 0／8／0／0／0 ms，正常退出码 0。

产物目录 `/tmp/voidmei-package-smoke-bm3rjxp5/` 保留启动日志、配置、CSV、报告与 `hud-scene.png`。截图已检查五区读数、消息、格号 F6、准星及不同背景 alpha。首轮监测未区分窗口初始尺寸与配置画布尺寸，后明确从达到目标尺寸开始；另一次数据验收虽通过，截图被设置窗口遮挡，因此在无窗口管理器的隔离环境中增加测试专用前置动作，并加强像素断言后重跑。这个测试不验证实际窗口管理器的置顶行为、真机 GPU 性能或游戏覆盖。

运行示例（需 JDK、Xvfb、xcompmgr 环境）：

```sh
TMPDIR=/tmp python3 script/test_hud_compositing.py python3 script/smoke_kmp_recording.py \
  /tmp/voidmei-kotlin-offline --hud-scene --compatible-hud --graceful-exit \
  --check-ui --renderer SOFTWARE_FAST --poll-interval-ms 80 --delayed-sample
```

Python 语法检查及既有四项渲染判定测试通过。本轮未触发远程 CI。

## 独立俯仰与横滚读数

飞行字段新增 `pitch`（俯仰，抬头为正）与 `roll`（横滚），保留一位小数，可在全局或单个飞行区域选择，不要求显示水平仪。复用现有 AttitudeGeometry 换算：俯仰取游戏姿态输入的相反数，横滚归一化到 [-180°, 180°)。两轴分别判断有效性，缺少横滚不隐藏有效俯仰，反之亦然；默认字段和已验证的水平仪绘制方向保持。

共享 JVM／JS、桌面单元和相关 GUI 回归通过（`/tmp/voidmei-hud-attitude-fields.log`，12 秒）。覆盖负原始俯仰对应正显示值、横滚归一化、俯仰边界、零与非法值、单轴缺失、字段选择／配置往返及区域实际读数。原有独立字段与预览回归继续通过。本轮未重建 Nix 包，未新增真机姿态验收。

## 区域自定义标题

每区可设置最多 80 字符的单行标题，留空时保持原展示。标题位于区域内容顶部，跟随该区内容透明度、显隐、滚动和层叠顺序；准星标题叠加在顶部，不移动准星中心。编辑预览边框优先显示标题，留空时仍显示类型，隐藏标记保持。标题作为区域属性保存，旧配置缺少 title 时默认空字符串，拒绝控制字符、过长标题和非字符串值。

共享 JVM／JS、桌面单元及定向 GUI 回归通过（`/tmp/voidmei-hud-region-titles.log`，11 秒），覆盖旧配置默认、非法值、位置／尺寸编辑保留、配置往返，以及实际标题输入后 HUD 和编辑预览同步、读数保持与清空恢复。既有区域管理、恢复和准星回归继续通过。

完整 GUI 回归通过：89 类、226 项，零失败／错误／跳过，46 秒（`/tmp/voidmei-hud-titles-full-gui.log`）。默认 Kotlin 离线 Nix 包构建通过（`/tmp/voidmei-hud-titles-nix.log`），包含此前独立俯仰／横滚读数。本轮未新增真实游戏验收。

## 编号发动机的可选压力单位

发动机字段新增 `manifold_inhg` 与 `boost_psi`，可全局选择或在各发动机区域独立配置。两者仅使用该区域匹配编号的 manifoldPressureAtm，复用既有压力换算；psi 标为“相对1atm”，不代表相对当前环境压力。低于 1 atm 时保留负增压，缺失或无效绝对压力显示未知。原 atm 字段也统一使用相同有效性判断。旧默认字段列表保持，不自动增加重复压力读数。

共享 JVM／JS、桌面单元及相关 GUI 回归通过（`/tmp/voidmei-hud-engine-pressure.log`，12 秒）。验证转换数值、负增压、1 atm 零增压、非法值、字段选择和配置往返；双发动机区域分别显示 1.50 atm 与 15.0 inHg／-7.3 psi，移除 2 号数据后仅该区显示不可用，1 号读数保持。本轮未重建 Nix 包，未新增真实游戏验收。

## 告警标题与列表共享区域高度

独立告警区域改用有界列布局：标题最多两行，超长省略，列表使用扣除标题实际高度与间距后的剩余空间。移除外层滚动，只保留告警列表自身滚动及提示；滚动告警时标题保持可见。没有标题时仍使用完整内容高度；非飞行连接状态继续沿用原分区提示。

桌面单元及相关 GUI 回归通过（`/tmp/voidmei-hud-alert-title-height.log`，7 秒），验证长标题、1.5×／2× 字体下列表完全位于区域边界内，最后一条可滚动到达、标题保持显示且只有一个滚动容器。原有区域高度、无告警隐藏、断线提示和标题编辑回归继续通过。本轮未重建 Nix 包，未新增真实游戏验收。

## 地图按实际文字高度布局

地图分区移除固定文字高度预算和外层滚动。标题最多两行，读数与距离标尺按实际文字高度排版，图面使用有界列布局的剩余空间；仍按地图宽高比居中绘制，因此剩余矩形与地图比例不同时保留空白。标尺按实际图面投影计算，不随标题变化误用旧尺寸。设置页地图保留原尺寸和交互。

桌面单元与相关地图 GUI 回归通过（`/tmp/voidmei-map-measured-height.log`，8 秒），验证 240×500 dp、2× 字体和长标题下，图面自动缩小、玩家位置与标尺保持在区域内、清空标题恢复图面高度且没有外层滚动。既有 240×400 dp 多档字体、共享地图、异常清除、原生兼容像素及设置页地图回归继续通过；更新后的 `desktop/build/hud-preview/map-regions-native.png` 已检查。

默认 Kotlin 离线 Nix 包构建通过（`/tmp/voidmei-map-measured-height-nix.log`），包含近期告警标题高度修正及编号发动机压力单位。本轮未新增真实游戏验收。

## 独立机械化百分比读数

飞行字段新增 `gear`、`flaps`、`airbrake`，可在全局或分区独立选择，不要求显示组合机械化面板。保留一位小数，0% 与 100% 有效，缺失、非有限或超出 0–100% 的值显示未知；默认字段保持。有效展开读数复用活动起落架／襟翼限速提示；减速板提示还检查当前开度至少 90%、起落架未完全放下，与既有告警条件一致。收起或无效读数不保留旧告警高亮。

共享 JVM／JS、桌面单元及相关 GUI 回归通过（`/tmp/voidmei-hud-mechanization-fields.log`，11 秒）。验证原始百分比来源、边界和非法值、字段保存，以及三个独立读数的告警语义、起落架放下后解除减速板提示、收起／缺失清除高亮；原字段选择和预览回归继续通过。本轮未重建 Nix 包，未新增真实游戏验收。

## 每区独立读数列数

飞行和发动机区域可选择“继承全局／自动／单列／双列”。共享 readingColumns 为 null 时继承全局，0 表示独立自动，1／2 为指定列数；旧配置缺失时继承。渲染通过区域内部的读数布局设置覆盖全局，复用原有文字宽度测量与自动列数算法，不影响邻区或非读数组件。

共享 JVM／JS、桌面单元和相关 GUI 回归通过（`/tmp/voidmei-hud-region-columns.log`，12 秒），验证各配置值往返与非法值拒绝；实际设置按钮切换列数，读数坐标验证单／双列、全局变化只影响继承区、独立自动模式及恢复继承。独立字段、标题与区域管理回归继续通过。本轮未重建 Nix 包，未新增真实游戏验收。

## 复制区域配置

“复制此区域”保留类型、尺寸、字段、发动机编号、透明度、显隐、标题和列数，分配未占用 ID，放在原区域上一层。位置尽量向右下错开 16 dp，靠近边界时向内错开，始终保持在画布内；铺满画布时允许重叠。副本与原区域后续独立编辑，不自动切换发动机编号；32 区域时禁用复制。

共享 JVM／JS、桌面单元和相关 GUI 回归通过（`/tmp/voidmei-hud-region-duplicate.log`，11 秒）。验证完整属性与顺序、唯一 ID、边界、铺满画布、数量上限及配置往返；实际复制后修改副本发动机编号、标题和列数，原区域保持。既有删除恢复及区域管理回归继续通过。

完整 GUI 回归通过：93 类、232 项，零失败／错误／跳过，47 秒（`/tmp/voidmei-hud-duplicate-full-gui.log`）。默认 Kotlin 离线 Nix 构建通过（`/tmp/voidmei-hud-duplicate-nix.log`），包含近期独立机械化读数和区域列数。本轮未新增真实游戏验收。

## WEP 估算说明

选择 WEP 燃料或续航字段时，HUD 显示估算上限说明：连接前已用量未知，按模型容量扣除观察到的消耗，断流后重新估算不表示实际补满。同一区域同时选择两个字段只显示一次说明。缺少匹配模型、暂无有效估算、当前未观测到 WEP 消耗分别提示；移除 WEP 字段后说明也移除。

桌面单元测试与 WEP、燃油续航 GUI 回归通过（`/tmp/voidmei-wep-status.log`）。新增测试使用实际 WepFuelMonitor，验证模型缺失／机型不匹配、持续消耗、关闭 WEP、油门数据缺失后清除旧读数，以及恢复采样时仍标明上限。这次未重建 Nix 包，未新增真实游戏验收。

## 独立罗盘区域

新增罗盘分区，可单独排列、调整尺寸、背景／内容不透明度、标题及显示状态。罗盘按标题剩余的实际区域尺寸绘制，沿用全局北向固定／航向朝上设置；不依赖飞行读数是否选择航向字段，也不改变原有读数所附的罗盘。缺少有效航向时显示“航向未知”，遥测延迟时清除旧罗盘并显示连接状态。继续使用同一个 HUD 顶层窗口与共享遥测。

共享 JVM／JS、桌面单元及六项相关 GUI 测试通过（`/tmp/voidmei-hud-compass-region.log`），覆盖新增区域与小画布配置往返、位置和可用尺寸、方向模式、缺失输入、延迟恢复和单独隐藏；原有分区单窗口与 alpha 测试继续通过。

默认 Kotlin 离线 Nix 包构建通过（`/tmp/voidmei-hud-compass-region-nix.log`），包含独立罗盘与 WEP 估算说明。本轮未新增真实游戏验收。

## 姿态区域利用可用高度

姿态分区不再固定使用 110 dp 水平仪。先为区域标题、航向与姿态说明保留实际布局空间，再将剩余高度交给水平仪；长区域标题最多显示两行。移除分区外层滚动，区域变大时图形随之变大，缩回后恢复原尺寸。纵向 HUD 及其他调用继续沿用原有固定高度，姿态方向与绘制计算保持。

桌面单元及七项相关 GUI 回归通过（`/tmp/voidmei-hud-attitude-size.log`），覆盖增高／缩回、长标题、1.5 倍字体下读数可见、缺失姿态清除，以及像素颜色验证抬头朝向天空、低头朝向地面；原有机体／地面参考方向与分区单窗口回归继续通过。本轮未重建 Nix 包，未新增真实游戏验收。

## 机械化告警与缺失状态

机械化面板的减速板读数现在复用活动减速板告警进行高亮，要求当前有效减速板开度至少 90%、起落架未完全放下；数据缺失、非法、条件恢复或关闭该显示时清除高亮。襟翼开度条开启但开度未知时，明确显示“数据不可用”，避免只开启该条目时整个区域无声消失；真实 0% 继续显示空条。

桌面单元及三项相关 GUI 回归通过（`/tmp/voidmei-mechanization-status.log`），覆盖告警启停、90% 边界、起落架完全放下、缺失／越界／非有限输入、恢复、显示开关和独立机械化数值字段。此次未重建 Nix 包，未新增真实游戏验收。

## 每区独立选择机械化内容

机械化分区可以通过“此区域独立选择机械化内容”选择起落架、襟翼／自动后掠、减速板和襟翼开度条。默认 null 继承全局开关，开启独立选择时复制当前全局配置；空清单明确不显示内容，关闭独立选择恢复继承。复用区域 fields 保存，未知 ID 保留。内容变化重置该区滚动位置，其他区域保持独立。

共享 JVM／JS、桌面单元及三项定向 GUI 回归通过（`/tmp/voidmei-mechanization-region.log`），覆盖配置往返、空选择、独立开度条、全局变化及恢复继承。

首次完整 GUI 回归发现上一轮的缺失开度条提示使默认纵向 HUD 增加一行，底部姿态读数越界。现在仅在隐藏襟翼数值且开启开度条时显示额外“数据不可用”提示；襟翼数值可见时已有未知读数，不重复占行。

修复后完整 GUI 回归通过：98 类、238 项，零失败／错误／跳过（`/tmp/voidmei-mechanization-full-gui-after.log`）。最终默认 Kotlin 离线 Nix 包构建通过（`/tmp/voidmei-mechanization-region-nix-final.log`），包含近期姿态尺寸适配和机械化状态改动；未新增真实游戏验收。

## 按区域筛选游戏消息

消息区域支持独立选择事件／损伤类别，默认两类都显示。复用 fields 保存 event、damage，未知 ID 保留；空选择明确显示“未选择消息类别”。先在共享消息历史中筛选类别，再取最近接收的 5 条，所以大量事件不会仅因占据最后 5 个位置而遮掉仍在历史内的损伤。历史仍受共享会话的容量限制，不保证永久保留某类记录；各区域不新增轮询会话。过滤后为空和读取失败分别提示，失败继续展示匹配的已有记录。

桌面单元及消息筛选／共享会话 GUI 回归通过（`/tmp/voidmei-hud-message-filter.log`），覆盖双区域分类、筛选与数量限制顺序、失败保留、取消类别、编辑切换、未知 ID 和配置往返。本轮未重建 Nix 包，未新增真实游戏验收。

## 按区域筛选告警类别

告警分区可独立选择警告／提示，复用 fields 保存 warning、advisory；null 默认展示两类，空清单不匹配任何告警，未知 ID 保留。正常飞行时依据该区域筛选后的告警决定是否显示，避免其他类别的告警触发空背景。遥测延迟／断线时仍显示连接状态并清除旧告警，类别筛选不影响告警计算、语音或其他区域的读数高亮。

桌面单元及告警筛选／区域 GUI 回归通过（`/tmp/voidmei-hud-alert-filter.log`），覆盖两区分类、空选择隐藏、切换类别、配置往返、恢复默认两类、未知 ID，以及延迟／断线提示与恢复后的自动隐藏。本轮未重建 Nix 包，未新增真实游戏验收。

## 九类区域的离线包运行验证

整包 `--hud-scene` 场景扩展为飞行、发动机、机械化、消息、罗盘、准星、姿态、地图和告警九类区域。模拟输入包含姿态、航向和减速板展开状态；保存后核对消息、告警和机械化的独立 fields。原生截图除了地图／准星黄色像素，还检查指定姿态区域的天空／地面颜色以及罗盘区域指针，避免仅以配置存在证明图形显示。

离线 Nix 包构建通过（`/tmp/voidmei-nine-regions-build.log`）。隔离 Xvfb/xcompmgr、SOFTWARE_FAST 兼容 HUD、80 ms 轮询、1.5 秒模拟延迟及正常关闭测试通过（`/tmp/voidmei-nine-regions-package.log`）。保留产物 `/tmp/voidmei-package-smoke-wgpxis1r/`：87 对 CSV 样本；35 次检查均为同一 900×600、位置 (40,40) 的 HUD 窗口；天空 26600、地面 9549、罗盘指针 137 个目标颜色像素。已查看实际截图，九类区域及透明间隙可见。五次 AWT 心跳延迟为 3、0、0、0、5 ms；不是帧率或实际游戏性能测量。

脚本语法检查及四项 Python 回归通过。此结果覆盖截至告警分类提交的生产代码，不扩大真实游戏、物理多显示器或原生 Wayland 验收结论。

## 精确输入区域位置与尺寸

区域设置新增可展开的“输入精确位置与尺寸”，以整数 dp 输入 X、Y、宽度和高度。四项同时校验并一次应用，区域必须完整位于当前画布内；非法、越界和没有变化时禁用应用，不在输入中间状态移动实际 HUD。标题、透明度等其他属性变化保留草稿及最新属性；拖动或画布收缩改变实际几何值时，同步对应输入。收起输入区会丢弃未应用草稿。

桌面单元及六项相关 GUI 回归通过（`/tmp/voidmei-region-geometry.log`），覆盖同时移动并缩小至边界、空值／负数／小数／溢出值、并发样式变化、配置往返、画布缩小后重新校验及原有区域设置操作。本轮未重建 Nix 包，未新增真实游戏验收。

## 质量估计与燃油占比刻度说明

选择质量估计或燃油质量占比时，HUD 显示一份可见说明：模型基础质量加燃油，不计弹药、外挂和损伤；缺少有效模型基础质量和缺少燃油量分别提示。选择燃油占比时额外标明进度条满刻度为 50%，超过刻度不截断数值读数，避免把满条误解为 100%。未选择相关字段时不显示说明。

桌面单元及两项 GUI 回归通过（`/tmp/voidmei-hud-mass-status.log`），覆盖 75% 超刻度数值、模型不匹配后清除旧图形、缺失燃油、真实零值、两字段说明去重与取消选择；WEP 估算说明回归继续通过。本轮未重建 Nix 包，未新增真实游戏验收。

## 速度限制比例说明

速度限制条无法计算时，区分缺少有效模型／机翼构型、缺少 IAS 或 Mach，以及计算结果不可用；不保留旧图形。有效时说明比例取 IAS/VNE 与 Mach/MNE 的较大值，满刻度 100%，超出刻度仍保留实际数值。沿用当前计算要求，不因只剩单侧输入而推断完整限制比例。

桌面单元及两项 GUI 回归通过（`/tmp/voidmei-speed-limit-status.log`），覆盖 IAS／Mach 主导切换、125% 读数、Mach 缺失、模型机型不匹配、模型 MNE 缺失、真实零速、字段隐藏及质量估计说明回归。本轮未重建 Nix 包，未新增真实游戏验收。

## 每区独立文字缩放

区域增加可选 fontScale，默认 null 继承全局 HUD 文字大小；设置提供 75%–200% 的独立档位。独立值替代全局 HUD 缩放因子，继续尊重系统字体缩放与画布显示缩放，不改变区域 dp 几何。恢复继承后跟随全局。复制区域和配置往返保留独立值，旧配置缺失该键时继承，非法／非有限值及字符串类型拒绝。

共享 JVM／JS、桌面单元及三项 GUI 回归通过（`/tmp/voidmei-region-font-after.log`），验证真实读数字体增大、全局变化隔离、恢复继承、区域边界不变，并回归姿态和罗盘尺寸行为。首次测试使用字符串替换修改格式化 JSON 时未命中，改为规范化 JSON 后执行类型拒绝验证。本轮未重建 Nix 包，未新增真实游戏验收。

## 指定预览拖动目标

预览编辑模式新增“拖动目标”菜单，默认自动选取重叠处顶层，也可按区域标题／ID 指定目标，包含隐藏区域。指定后在该区域范围内拖动或拖动其右下角进行编辑，不改变层叠顺序；区域外拖动不会误选其他区域。菜单仅存在于编辑预览，实际 HUD 仍统一穿透。目标是预览临时状态，不写入布局；移除目标区域后恢复自动选择。

桌面单元和拖动 GUI 回归通过（`/tmp/voidmei-drag-target.log`），覆盖菜单选择被覆盖底层、半比例坐标转换、区域外不误移、恢复自动顶层选取，以及原有真实预览窗口移动／缩放操作。本轮未重建 Nix 包，未新增真实游戏验收。

## 布局变化取消旧拖动手势

预览拖动输入现在以目标 ID 和区域 ID 列表作为手势生命周期条件。按压过程中切换目标、删除／添加／重排区域时，取消旧手势，重新按下才开始编辑；普通遥测或拖动自身的位置更新不重启手势。避免仍按住鼠标时沿用旧目标继续修改。画布或缩放变化继续沿用已有取消规则。

桌面单元及五项拖动回归通过（`/tmp/voidmei-drag-cancel.log`），包含跨多个输入批次的持续按压、切换目标后停止修改、重新按压恢复，以及删除目标后不误改剩余区域。完整 GUI 回归通过：104 类、246 项，零失败／错误／跳过（`/tmp/voidmei-layout-full-gui.log`）。最新离线 Nix 包构建通过（`/tmp/voidmei-layout-nix.log`），包含近期区域文字缩放、精确位置输入、派生读数说明和拖动目标配置。本轮未新增实际整包启动或真实游戏验收。

## 发动机油门与转速的无效值一致性

独立发动机读数现在与飞行读数一致，负油门和负转速显示未知，非有限输入仍不显示；0 和超过 100% 的油门继续保留。无效转速也不再触发读数高亮。未扩展到其他有符号通道，负温度、磁电机原值保持。

共享 JVM／JS、桌面单元及发动机 GUI 回归通过（`/tmp/voidmei-engine-validity.log`），覆盖两套字段的缺失、负值、非有限值、零值与有效值一致性，以及旧转速／高亮清除和恢复。本轮未重建 Nix 包，未新增真实游戏验收。

## 命名布局预设

分区设置增加“管理布局预设”，最多保存 16 套命名画布／区域配置，可载入、替换同名布局和删除预设。名称去除首尾空白后保存，限制 80 字符且不能包含控制字符。预设保存每区字段、字体缩放、透明度、发动机编号、标题、位置等已有配置；继承项继续沿用当前全局设置，不复制全局字体、颜色、连接或记录偏好。载入启用对应分区布局，删除预设不改变正在使用的布局。

AppSettings 新增 hudScenePresets，旧配置默认空集合。共享 JVM／JS、桌面单元及六项相关 GUI 回归通过（`/tmp/voidmei-layout-presets-after.log`），覆盖预设往返、非法名称／数量限制、保存后修改当前布局、载入保留全局偏好、同名替换、删除不影响当前布局和原有设置操作。首次旧配置样本遗漏必需版本字段，补齐后验证通过。本轮未重建 Nix 包，未新增真实游戏验收。

## 从纵向模式载入预设

已有预设时，即使当前没有分区布局或正在使用纵向模式，也保留预设管理入口，可直接载入。没有当前分区配置时禁用保存按钮；载入继续遵守原有平台支持条件。模式切换不再卸载预设管理内容，已展开的列表可继续操作；不改变 HUD 总开关、全局字体和用户穿透偏好。

桌面单元及七项相关 GUI 回归通过（`/tmp/voidmei-presets-vertical-after.log`），新增覆盖无当前布局直接载入、切回纵向再载入和全局偏好保留，原有预设增删／替换及分区设置回归继续通过。本轮未重建 Nix 包，未新增真实游戏验收。

## 预设载入清除旧区域恢复记录

载入预设现在显式清除“恢复上次移除区域”的临时记录，避免把上一套布局中删除的区域误恢复到新布局。即使载入的几何配置等于当前配置，也视为新的恢复上下文；保存、替换和删除预设不触发这项清理。

桌面单元及四项相关 GUI 回归通过（`/tmp/voidmei-preset-restore-context.log`），验证删除区域后保存仍可恢复、载入同一布局后清除恢复入口，以及既有恢复适配画布、预设操作与纵向载入流程。本轮未重建 Nix 包，未新增真实游戏验收。

## 大型预设集合的实际文件保存

新增文件级测试发现，16 套各含 32 个区域、全部飞行字段和长标题的合法配置，会因格式化缩进超过 1 MiB 而保存失败（`/tmp/voidmei-preset-storage-before.log`）。SettingsStore 现在先生成可读格式，超出上限时使用共享编码器生成紧凑 JSON；紧凑后仍超限则继续拒绝写入。保持未知键、外部修改检测及原子文件替换，小型配置仍使用可读格式。

共享 JVM／JS 和完整桌面单元测试通过（`/tmp/voidmei-preset-storage-final.log`），实际临时文件验证完整预设保存、重启读取、后续修改、未知键保留，以及已有超限／无效编码／外部修改保护。新增共享紧凑编码往返在 JVM／JS 上通过（`/tmp/voidmei-preset-compact-shared.log`）。本轮未重建 Nix 包，未新增 GUI 或真实游戏验收。

## 离线包预设持久化验证

九区域整包测试增加两套命名预设，其中未启用预设包含独立字段、未知字段 ID、标题、列数和文字缩放。正常退出后验证两套配置及当前布局一致性，并检查编码器补出的默认字段，区分真实预设解析与未知 JSON 原样保留。

最新离线 Nix 包构建通过（`/tmp/voidmei-presets-package-build.log`）。隔离 Xvfb/xcompmgr、SOFTWARE_FAST 兼容 HUD、80 ms 轮询、1.5 秒模拟延迟及正常退出测试通过（`/tmp/voidmei-presets-package-run.log`）。产物 `/tmp/voidmei-package-smoke-ir9rwny3/`：86 对记录样本，34 次检查保持同一 900×600 HUD 窗口，姿态天空／地面和罗盘指针像素检查通过。两套预设解析和保存检查通过；这不是 GUI 菜单点击测试。脚本语法与四项 Python 回归通过。本轮未新增真实游戏验收。

## 撤销最近一次预设载入

预设管理现在保留本次页面最近一次载入前的布局，可通过“撤销上次载入”恢复；包括 null 或停用分区时的纵向模式。收起管理面板不丢失该记录，下一次载入覆盖记录，撤销后清空。仅恢复布局，保留之后修改的全局偏好与预设列表。撤销同样清除跨布局的已移除区域恢复记录。

桌面单元及五项 GUI 回归通过（`/tmp/voidmei-preset-load-undo.log`），覆盖恢复 null／停用布局、收起后撤销、之后字体修改与预设删除保留，并回归载入、保存、替换和区域恢复上下文。本轮未重建 Nix 包，未新增真实游戏验收。

## 载入预设重置未应用输入

载入及撤销载入预设时增加编辑代次，重建区域编辑状态和画布尺寸输入。即使区域 ID、坐标与尺寸完全相同，也丢弃此前未应用的草稿，避免把旧布局输入误应用到新布局。普通样式修改不改变编辑代次，继续保留当前草稿。

桌面单元及五项 GUI 回归通过（`/tmp/voidmei-preset-drafts-after.log`），验证相同布局载入／撤销后区域坐标和画布宽度草稿清除，精确几何编辑及预设流程继续通过。首次断言把字段标签与编辑值混合比较，改为只检查 EditableText 后通过。本轮未重建 Nix 包，未新增真实游戏验收。

## 独立操纵面区域

新增操纵面分区，显示副翼、升降舵、方向舵的有符号百分比和 −100% 至 +100% 的位置刻度。零值居中，缺失／越界／非有限值显示未知且不画位置点，避免误表示为中立。界面说明百分比不代表实际偏转角。沿用区域位置、尺寸、透明度、字体及同一遥测会话；普通预览补入三轴示例，缺失数据预览仍不补零。

共享 JVM／JS 配置回归通过（`/tmp/voidmei-controls-config.log`），覆盖正常／小画布新增与保存；桌面单元及六项相关 GUI 回归通过（`/tmp/voidmei-controls-final.log`），像素检查负／零／正位置和无效输入清除，原有分区回归继续通过。本轮未重建 Nix 包，未新增真实游戏验收。此前九区域整包测试不包含本区域。

## 操纵面读数与图形使用一致范围

将有符号操纵面百分比校验提取到共享层，飞行字段与独立图形区域统一接受 −100% 至 +100%，越界、缺失及非有限输入显示未知，不把未知变成中立。遥测原始对象和记录数据保持原值。

共享 JVM／JS、桌面单元及操纵面图形 GUI 回归通过（`/tmp/voidmei-controls-validation.log`）。共享测试覆盖三轴、端点、正负方向、零、越界和非有限值，并使用真机快照中的 0／67／−32 百分比验证解析后的显示值。本轮未重建 Nix 包，未新增真实游戏验收。

## 每区选择操纵面轴

操纵面分区可独立选择副翼、升降舵和方向舵。fields 为 null 时显示全部三轴，空清单明确显示“未选择操纵面”；选择按清单顺序显示，未知 ID 在编辑与保存中保留。可把不同轴放在不同区域，配置变化重置对应区域的滚动位置。

桌面单元及两项操纵面 GUI 测试通过（`/tmp/voidmei-control-axis-selection.log`），覆盖双区域不同选择、空选择、改选升降舵、未知 ID 保留、配置往返及恢复三轴。完整 GUI 回归通过：108 类、254 项，零失败／错误／跳过（`/tmp/voidmei-controls-full-gui.log`）。最新离线 Nix 包构建通过（`/tmp/voidmei-controls-nix.log`），包含近期操纵面分区和预设撤销／草稿修复；本轮未新增实际整包启动或真实游戏验收。

## 每区隐藏飞行读数附带图形

飞行区域新增“显示读数附带图形”开关，默认开启，旧配置保持原有显示。关闭后隐藏航向罗盘、迎角余量、油门、油量、燃油质量占比和速度限制附带组件，保留选定数值及其告警高亮；不同区域可分别配置。独立罗盘等区域不受此开关影响。

共享 JVM／JS 配置、桌面单元测试通过（`/tmp/voidmei-reading-instruments.log`）；最终两项 GUI 回归通过（`/tmp/voidmei-reading-instruments-final.log`），覆盖双区域隔离、开关恢复、配置往返与关闭图形后迎角数值仍有告警标记。本轮未重建 Nix 包，未新增真实游戏验收。

## 十区域离线整包验证

实际启动测试扩展到当前十种区域：缩小地图，为独立操纵面区域留出空间，选择升降舵并注入 +67% 遥测。截图在预期正向刻度位置检查绿色位置点；退出保存同时检查操纵面选择，以及未启用预设中的 showFlightInstruments=false。

离线包构建通过（`/tmp/voidmei-ten-regions-build.log`）。隔离 X11、SOFTWARE_FAST 兼容 HUD、80 ms 刷新及一次 1.5 秒响应延迟的整包测试通过（`/tmp/voidmei-ten-regions-package.log`），产生 86 对记录行；34 次采样保持同一个 900×600 HUD 窗口。升降舵位置点 36 像素，天空 26600、大地 9549、罗盘 137 像素；AWT 心跳延迟 5／0／0／7／0 ms，正常退出。人工查看截图确认操纵面区域与其他区域共同绘制。产物位于 `/tmp/voidmei-package-smoke-gke1aaep/`。Python 语法检查及四项脚本单元测试通过。本验证不代替真实游戏、物理多显示器或 GPU 后端验收。

## 空预设列表保留编辑与撤销入口

HUD 设置始终保留布局预设组件，避免“载入预设 → 返回纵向布局 → 删除最后一个预设”移除整个组件并丢失载入撤销记录。纵向模式下仍可保存保留的停用分区布局；当前没有布局时保存按钮禁用。

完整设置页回归先复现名称输入框消失（`/tmp/voidmei-empty-presets-before.log`），修复后验证删除最后一个预设、另存停用布局、再次删除及撤销回到 null 布局。桌面单元和预设／草稿／区域恢复 GUI 回归通过（`/tmp/voidmei-empty-presets-after.log`）。本轮未重建 Nix 包或新增真实游戏验收。

## 十区域连接状态切换回归

新增完整 HudPanel 的十区域 GUI 回归，依次从有效飞行切换到遥测延迟、断线、连接中和等待出击，再恢复有效飞行。验证每个区域显示对应连接状态，旧姿态画布、罗盘、准星和操纵面组件移除，恢复后重新出现。随后切换缺失遥测，验证姿态／航向不可用、升降舵显示未知而非零值。

隔离 X11 GUI 测试通过（`/tmp/voidmei-scene-connection.log`）。本轮只增加回归证据，未修改运行逻辑，未重建 Nix 包；测试直接驱动连接状态，不覆盖 HTTP 轮询状态机或真实游戏重连。

## 空字段区域说明

飞行和机械化分区在没有选中任何已知字段时分别显示“未选择飞行读数”“未选择机械化内容”，避免空区域被误认为遥测停止。仅在飞行状态显示该提示，等待／断线仍显示连接状态；选择有效字段或恢复全局继承后提示消失。未知字段仍由原有配置机制保留。

桌面单元与空区域、十区域连接状态 GUI 回归通过（`/tmp/voidmei-empty-regions.log`），覆盖空列表、未知字段、恢复选择与继承及等待出击。本轮未重建 Nix 包，未新增真实游戏验收。

## 近期分区改动完整回归

在 cfa6620 状态运行共享 JVM／JS、桌面单元和完整隔离 X11 GUI 回归（`/tmp/voidmei-scene-complete-regression.log`）。GUI 本轮重新执行，111 类、259 项测试，零失败／错误／跳过，构建耗时 49 秒。共享 JVM 468 项、JS 465 项及桌面单元 137 项为 Gradle UP-TO-DATE 有效结果，未宣称本轮重新执行。

检查首次启用布局实现：发动机编号、姿态／机械化开关及区域背景透明度从当前设置生成；字段为 null，继续继承全局选择。本轮未修改运行代码，未重建 Nix 包，也未新增真实游戏或物理多显示器验收。

## 布局预设直接重命名

预设管理使用上方布局名称输入框作为目标名称，每套预设提供“重命名为输入名称”。沿用名称校验和首尾空白清理，目标已存在时禁用，避免覆盖。重命名保留原预设内容和列表顺序，不把当前编辑布局写入预设，也不修改当前布局或载入撤销记录。

桌面单元和八项预设／草稿／区域恢复 GUI 回归通过（`/tmp/voidmei-preset-rename.log`）。新增测试覆盖空名、名称冲突、首尾空白、顺序与原始内容保留、当前布局隔离、配置往返及改名后载入。本轮未重建 Nix 包，未新增真实游戏验收。

## 操纵面区域排序

操纵面字段设置新增可收起的顺序编辑，每个已选轴可上移／下移。操作交换相邻可见轴，保留清单中的未知字段；首尾操作禁用，HUD 按新顺序绘制。默认收起，避免增加常规字段选择的页面高度。

桌面单元及三项操纵面 GUI 回归通过（`/tmp/voidmei-control-order.log`），覆盖边界按钮、上移／下移、实际图形纵向位置变化、未知字段和配置往返，并回归独立区域选轴与无效数据绘制。本轮未重建 Nix 包，未新增真实游戏验收。

## 短暂遥测延迟保留共享地图会话

应用级地图会话记住最近的飞行身份，Delayed 不再重建会话；真实断线、切机、端点变更及手动重连仍取消旧作用域。地图采用按需共享，并在最后一个消费者离开后保留 5 秒，以跨过延迟状态下 HUD 临时移除订阅的空档；超过保留期停止读取并清空缓存，之后订阅重新启动。首次无人使用地图时不会主动请求。

桌面单元及地图会话／十区域连接状态 GUI 回归通过（`/tmp/voidmei-map-session-delay.log`、`/tmp/voidmei-map-session-final.log`）。测试覆盖双消费者共用读取器、短暂延迟移除消费者但保留会话、恢复、断线释放、切机／换端点／手动重连，以及超过保留期停止和恢复订阅。测试使用注入流验证生命周期，未宣称真实 HTTP／游戏验收。本轮未重建 Nix 包。

## 地图会话修复后的离线整包回归

包含 cf2eb6f 及近期预设重命名、操纵面排序的离线包构建通过（`/tmp/voidmei-map-session-package-build.log`）。隔离 X11／SOFTWARE_FAST 兼容 HUD、十区域、80 ms 刷新和一次 1.5 秒遥测延迟的整包测试通过（`/tmp/voidmei-map-session-package-run.log`）。

35 次采样保持一个 900×600 HUD 窗口；升降舵 36、天空 26600、大地 9549、罗盘 137 像素。88 对 CSV 数据行、9 次地图对象请求，AWT 心跳 0／0／0／7／0 ms，正常退出，配置及预设保存检查通过。产物 `/tmp/voidmei-package-smoke-esx53em1/`。本轮验证真实进程集成，不从请求总数推断地图会话未重建，也不替代真实游戏与 GPU 后端验收。

## 独立罗盘显示数值航向

独立罗盘在图形上方显示三位航向角（如 090°），使用区域字体和内容透明度。取整跨过北向时显示 000°，与图形的无障碍航向描述一致；缺失时只显示“航向未知”。罗盘图形继续使用文字下方的剩余高度。

桌面单元及独立罗盘、十区域连接状态 GUI 回归通过（`/tmp/voidmei-compass-heading-reading.log`），覆盖 090°、359.6° 取整为 000°、图形方向描述一致、尺寸调整、缺失清除、延迟恢复及隐藏。本轮未重建 Nix 包，未新增真实游戏验收。

## 每区配置消息显示条数

消息分区新增 messageLimit，默认 5，支持 1 至 20；设置提供 1／3／5／10／20 条选项。标题显示当前限制，先按消息类别筛选再取最近记录；修改限制重置区域滚动位置。共享历史仍最多 200 条，显示限制不修改游标或其他区域。旧配置缺省为 5，错误类型与越界值拒绝。

共享 JVM／JS、桌面单元和消息相关 GUI 回归通过（`/tmp/voidmei-message-limit.log`）。覆盖配置往返、旧值缺省、无效输入，以及两个区域独立限制、筛选顺序、从 1 条扩展到 10 条、错误保留历史和共享会话行为。本轮未重建 Nix 包，未新增真实游戏验收。

## 飞行区域可隐藏重复状态标题

飞行区域新增 showFlightStatus，默认 true。关闭后省略有效飞行时的“实时 · 机型”或自定义连接标题，保留选定读数和自定义区域标题。非飞行状态仍始终显示延迟、断线、连接中或等待提示，避免精简布局掩盖连接异常。

共享 JVM／JS、桌面单元及三项精简读数 GUI 回归通过（`/tmp/voidmei-flight-status-toggle.log`）。测试覆盖双区域开关隔离、数值保留、延迟／断线／等待状态及恢复、旧配置默认值和序列化。本轮未重建 Nix 包，未新增真实游戏验收。

## 新区域选项的保存与重置回归

扩展真实 SettingsStore 文件回归，在 16 套预设、每套 32 区域的大配置中使用关闭飞行标题／附带图形及 20 条消息的非默认值。首次保存、新实例加载、修改音量后再次保存均比较完整配置，并保留未知根键和文件大小检查。

新增共享层操作链回归：重置当前布局保留预设，之后修改预设名称／停用状态及音量，再撤销布局重置，恢复原区域选项且保留后续独立修改。共享 JVM／JS 和桌面单元测试通过（`/tmp/voidmei-region-options-persistence.log`）。本轮仅补充验证，未修改运行代码或重建 Nix 包。
