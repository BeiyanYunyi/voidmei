package voidmei.desktop

import androidx.compose.material3.darkColorScheme
import androidx.compose.ui.graphics.Color

/** Shared by the live HUD and its layout preview so uncustomized gauges match. */
internal fun hudColorScheme() = darkColorScheme(primary = Color(0xFF84DEC6))
